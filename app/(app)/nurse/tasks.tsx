/**
 * This file intentionally has no default export.
 *
 * The nurse tasks screen lives at tasks/index.tsx, wrapped by tasks/_layout.tsx.
 * Expo Router uses the tasks/ directory as the canonical route — this file
 * is kept only as a placeholder to avoid git conflicts; it is not a route.
 */
